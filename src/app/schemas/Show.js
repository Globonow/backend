import { Schema, model } from 'mongoose';

const ShowSchema = new Schema(
  {
    name: {
      type: String,
      required: true,
    },
    image: {
      type: String,
      required: true,
    },
    description: {
      type: String,
      required: true,
    },
  },

);

export default model('Show', ShowSchema);
